# textclassification

A Python project for text classification.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate  # On Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

## Usage

```bash
python -m textclassification
```

## Development

```bash
pip install -e ".[dev]"
pytest
```
